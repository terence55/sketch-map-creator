@import "baidu.js";
@import "mapbox.js";
@import "google.js";

function commandCreateBaiduMap(context) {
  var map = new BaiduMap();
  map.createMap(context);
}

function commandCreateMapboxMap(context) {
  var map = new Mapbox();
  map.createMap(context);
}

function commandCreateGoogleMap(context) {
  var map = new Google();
  map.createMap(context);
}